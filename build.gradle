subprojects {
  apply plugin: 'java'

  repositories {
    jcenter()
  }
  
  dependencies {
  	   // https://mvnrepository.com/artifact/com.google.guava/guava
    compile 'com.google:guava', name: 'guava', version: '24.0-jre'
  }
}
